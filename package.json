{
  "name": "esingesini-lounge-operations",
  "version": "2.4.0",
  "private": true,
  "description": "Production business operations, POS, unified stock synchronization, supplier pricing, markup, accounting, Capitec merchant readiness and strict executive access control for Esingesini Lounge.",
  "main": "system-server.js",
  "scripts": {
    "start": "node -r ./access-preload.js -r ./accounting-preload.js -r ./integration-preload.js -r ./payment-preload.js system-server.js",
    "check": "node --check access-preload.js && node --check accounting-preload.js && node --check integration-preload.js && node --check payment-preload.js && node --check system-server.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "pg": "^8.16.3"
  }
}

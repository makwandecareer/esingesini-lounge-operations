{
  "name": "esingesini-lounge-operations",
  "version": "2.2.0",
  "private": true,
  "description": "Production business operations, POS, unified stock synchronization, supplier pricing, markup, accounting, cash-up and management system for Esingesini Lounge.",
  "main": "system-server.js",
  "scripts": {
    "start": "node -r ./accounting-preload.js -r ./integration-preload.js system-server.js",
    "check": "node --check accounting-preload.js && node --check integration-preload.js && node --check system-server.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "pg": "^8.16.3"
  }
}
